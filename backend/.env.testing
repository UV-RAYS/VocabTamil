# VocabTamil Testing Environment Variables
# SAFE FOR TESTING - NO REAL SECRETS

# Django Settings
SECRET_KEY=testing-secret-key-for-development-only
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,beta.vocabtamil.com,testserver

# Database (Use SQLite for testing)
DATABASE_URL=sqlite:///test_vocabtamil.db
DB_PASSWORD=test-db-password

# Redis Cache (Use fake Redis for testing)
REDIS_URL=redis://localhost:6379/1

# AWS S3 Storage (Dummy values - will be mocked)
AWS_ACCESS_KEY_ID=test-access-key
AWS_SECRET_ACCESS_KEY=test-secret-key
AWS_STORAGE_BUCKET_NAME=test-bucket
AWS_S3_REGION_NAME=us-east-1
AWS_S3_USE_SSL=False

# Google OAuth (Dummy - will be mocked)
GOOGLE_OAUTH2_CLIENT_ID=test-client-id.apps.googleusercontent.com
GOOGLE_OAUTH2_CLIENT_SECRET=test-client-secret

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8000/api
NEXT_PUBLIC_APP_NAME=VocabTamil-Test

# Test Superuser
DJANGO_SUPERUSER_USERNAME=testadmin
DJANGO_SUPERUSER_EMAIL=testadmin@example.com
DJANGO_SUPERUSER_PASSWORD=testpassword123

# Email (Use console backend for testing)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=localhost
EMAIL_PORT=1025
EMAIL_USE_TLS=False
EMAIL_HOST_USER=test@example.com
EMAIL_HOST_PASSWORD=test-email-pass

# Monitoring & Analytics (Disabled for testing)
SENTRY_DSN=
ANALYTICS_ENABLED=False

# SSL (Disabled for testing)
SSL_CERT_PATH=
SSL_KEY_PATH=
USE_SSL=False

# Performance Testing Settings
SLOW_QUERY_THRESHOLD=2.0
RATE_LIMIT_ENABLED=False
CACHE_TIMEOUT=60

# Testing Flags
LOAD_SAMPLE_DATA=True
RUN_TESTS=True
MOCK_EXTERNAL_SERVICES=True
